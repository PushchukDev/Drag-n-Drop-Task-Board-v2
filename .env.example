# This app is fully client-side and needs no API keys or secrets.
# If you add external services later, put values here in a local `.env` file
# (never commit `.env`) and read them via Vite's `import.meta.env.VITE_*` vars.
#
# Example (placeholder only — do not put real secrets in this file):
# VITE_API_BASE_URL=https://api.example.com
